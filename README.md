# ByteLab

Upload `index.html` and `python.html` to the repository root. Keep `assets/bytelab-logo.svg` in the `assets` folder. The main page preserves the existing Google Apps Script registration/submission connection.
